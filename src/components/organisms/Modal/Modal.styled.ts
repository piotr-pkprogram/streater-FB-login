// @ts-ignore
import styled from 'styled-components';
// @ts-ignore
import ReactModal from 'react-modal';

export const ModalWrapper = styled(ReactModal).attrs({
  className: 'fixed top-0 w-full h-full z-40'
})``;
